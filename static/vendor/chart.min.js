/*!
 * Minimal Chart.js stub for offline use.
 */
(function(global){
    function Chart(ctx,config){
        this.ctx=ctx;
        this.config=config||{};
        this.data=this.config.data||{};
        this.options=this.config.options||{};
        console.warn('Chart.js stub initialized');
    }
    Chart.prototype.update=function(){console.warn('Chart.update called');};
    global.Chart=Chart;
})(typeof window!=='undefined'?window:this);
